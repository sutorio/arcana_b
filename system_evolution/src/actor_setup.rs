use bevy::prelude::*;
use bevy_rapier3d::prelude::*;

pub struct ActorSetupPlugin;

impl Plugin for ActorSetupPlugin {
    fn build(&self, app: &mut App) {
        unimplemented!("Need to add the plugin stuff!");
    }
}